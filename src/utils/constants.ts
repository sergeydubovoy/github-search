export const HEADING: string = "GitHub Search";

export const BUTTONS_TEXT: {
  copy: string;
  copied: string;
  more: string;
  addToFavorite: string;
  addedToFavorite: string;
  back: string;
} = {
  copy: "Copy",
  copied: "Copied",
  more: "More",
  addToFavorite: "Add to favorite",
  addedToFavorite: "Added to favorite",
  back: "Back",
};

export const INPUT_TEXT: { placeholder: string; name: string } = {
  placeholder: "Type to search repositories",
  name: "title",
};

export const NO_FAVORITE_REPOSITORIES: string = "No favorite repositories";

export const SHOW_PER_PAGE_CONST: string = "Show by: ";

export const ERRORS_TEXT: {
  noRepositoryTitle: string;
  noRepositoriesFound: string;
  noForks: string;
  noStars: string;
} = {
  noRepositoryTitle: "No repository title",
  noRepositoriesFound: "No repositories found",
  noForks: "0",
  noStars: "0",
};

export const SELECT_VALUES: {
  defaultValue: string;
  firstValue: string;
  secondValue: string;
  thirdValue: string;
  fourthValue: string;
} = {
  defaultValue: "12",
  firstValue: "12",
  secondValue: "24",
  thirdValue: "36",
  fourthValue: "72",
};

export const LIST_TITLES: { result: string; favorite: string } = {
  result: "Search result",
  favorite: "Favorite repositories",
};

export const DESCRIPTION_TITLES: { description: string; language: string } = {
  description: "Description",
  language: "Language",
};
